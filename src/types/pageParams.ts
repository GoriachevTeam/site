export type TypeArticlePageParams = {
  params: {
    slug: string;
  };
};

export type TypeCategoryPageParams = {
  params: {
    categorySlug: string;
  };
};

export type TypeServicePageParams = {
  params: {
    slug: string;
  };
};
